/* eslint-disable */
export type Methods = {
  /** Skips to previous track in the user’s queue. */
  post: {
    query?: {
      device_id?: string | undefined
    } | undefined

    status: 204
  }
}
